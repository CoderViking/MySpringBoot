<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.viking.MySpringBoot.mapper.FruitsMapper">
    <select id="getAllWeather" parameterType="java.lang.String" resultType="com.viking.MySpringBoot.entity.Weather">
        select * from weather
    </select>
    <insert id="insert" parameterType="com.viking.MySpringBoot.entity.Weather">
        INSERT INTO weather (weather,temperature,tip,date,user_name)
        VALUES (#{param.weather},#{param.temperature},#{param.tip},#{param.date},#{param.userName})
    </insert>
</mapper>