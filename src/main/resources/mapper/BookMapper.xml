<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.viking.MySpringBoot.mapper.BookMapper">
    <select id="listBook" resultType="java.util.Map">
        SELECT * FROM book ORDER by ISBN DESC limit 20
    </select>
</mapper>